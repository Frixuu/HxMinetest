package minetest.colors;

/**
	NamedColor is an RGB `Color` that can be referred to by a name.
**/
abstract class NamedColor {
	public static final red = Color.rgb(255, 0, 0);
	public static final black = Color.rgb(0, 0, 0);
	public static final white = Color.rgb(255, 255, 255);
}
